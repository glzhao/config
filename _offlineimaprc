# ft = conf

[general]
ui = ttyui
#accounts = work,lucienchao_at_gmail,glzhao1_at_gmail
accounts = lucienchao_at_gmail,glzhao1_at_gmail
maxsyncaccounts=2
pythonfile=~/.mutt/offlineimap.py
# Die after 120 seconds of nothing. Probably the connection died ...
# socktimeout = 120

[Account work]
localrepository = local-work
remoterepository = remote-work
# Minutes between syncs
autorefresh = 5
# Number of quick-syncs between autorefreshes. Quick-syncs do not update if the
# only changes were to IMAP flags
quick = 10

[Repository local-work]
type = Maildir
localfolders = ~/Mail/inbox/work
# Instead of closing the connection once a sync is complete, offlineimap will
# send empty data to the server to hold the connection open. A value of 60
# attempts to hold the connection for a minute between syncs (both quick and
# autorefresh).This setting has no effect if autorefresh and holdconnectionopen
# are not both set.
keepalive = 60

[Repository remote-work]
type = IMAP
remotehost = xxx
remoteport = 993
remoteuser = xxx
#remoteusereval = get_username("mail.sogou-inc.com")
remotepasseval = xxx
ssl = yes
#folderfilter = lambda foldername: foldername in ['INBOX', 'Sent Items', 'Tasklist', 'Calendar']
# nametrans = oimaptransfolder_acc1
# insert your server certificate's fingerprint here if needed
# # cert_fingerprint=1c671ac670b1fbbfddeddd3541dbcc06148061e2
# # But also see this thread:
# # http://comments.gmane.org/gmane.mail.imap.offlineimap.general/2312

[Account lucienchao_at_gmail]
localrepository = local-lucien
remoterepository = remote-lucien
autorefresh = 1
quick = 10

[Repository local-lucien]
type = Maildir
localfolders = ~/Mail/inbox/lucienchao_at_gmail
keepalive = 60

[Repository remote-lucien]
type = Gmail
ssl = yes
#remotehost = imap.gmail.com
#remoteport = 993
realdelete = no
remoteuser = lucienchao
remotepasseval = keyring.get_password('imap.gmail.com', 'lucienchao')
folderfilter = lambda foldername: foldername not in ['[Gmail]/All Mail', '[Gmail]/Trash']
# nametrans = lambda foldername: re.sub ('^\[gmail\]', 'bak',
#				re.sub ('sent_mail', 'sent',
#				re.sub ('starred', 'flagged',
#				re.sub (' ', '_', foldername.lower()))))
# Necessary as of OfflineIMAP 6.5.4
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
#
# nametrans = oimaptransfolder_acc1
# insert your server certificate's fingerprint here if needed
# # cert_fingerprint=1c671ac670b1fbbfddeddd3541dbcc06148061e2
# # But also see this thread:
# # http://comments.gmane.org/gmane.mail.imap.offlineimap.general/2312

[Account glzhao1_at_gmail]
localrepository = local-glzhao1
remoterepository = remote-glzhao1
autorefresh = 1
quick = 10

[Repository local-glzhao1]
type = Maildir
localfolders = ~/Mail/inbox/glzhao1_at_gmail/
keepalive = 60

[Repository remote-glzhao1]
type = Gmail
ssl = yes
remotehost = imap.gmail.com
remoteport = 993
realdelete = no
remoteuser = glzhao1
#remoteusereval = get_username("imap.gmail.com")
remotepasseval = keyring.get_password('imap.gmail.com', 'glzhao1')
folderfilter = lambda foldername: foldername not in ['[Gmail]/All Mail', '[Gmail]/Trash']
# nametrans = lambda foldername: re.sub ('^\[gmail\]', 'bak',
#				re.sub ('sent_mail', 'sent',
#				re.sub ('starred', 'flagged',
#				re.sub (' ', '_', foldername.lower()))))
# Necessary as of OfflineIMAP 6.5.4
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
#
# nametrans = oimaptransfolder_acc1
# insert your server certificate's fingerprint here if needed
# # cert_fingerprint=1c671ac670b1fbbfddeddd3541dbcc06148061e2
# # But also see this thread:
# # http://comments.gmane.org/gmane.mail.imap.offlineimap.general/2312
#remotepassfile = /path/to/passwordfile
